<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.helpticket">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".mainModel.ReportActivity"
            android:label="@string/title_activity_report"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".mainModel.DetailsActivity"
            android:label="@string/title_activity_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".mainModel.AllTicketActivity"
            android:label="@string/title_activity_all_ticket"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".mainModel.UnsolvedTicketActivity"
            android:label="@string/title_activity_unsolved_ticket"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".mainModel.MenuActivity" />
        <activity android:name=".mainModel.CreateTicketActivity" />
        <activity android:name=".mainModel.SolvedTicketActivity" /> <!-- Intent filter to catch Microsoft's callback after Sign In -->
        <!-- Intent filter to capture System Browser or Authenticator calling back to our app after sign-in -->
        <activity android:name=".ui.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.microsoft.identity.client.BrowserTabActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="com.example.helpticket"
                    android:path="/Bmce+9aHdOoVtE7fS3B07tfj7Bc="
                    android:scheme="msauth" />
            </intent-filter>
        </activity>
    </application>

</manifest>